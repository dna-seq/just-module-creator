# Void — killed mid-enrich, superseded by run-ards-a

Killed by an external quota limit during `enrich_module`, 2026-08-31. Kept rather than deleted
because its authored rows are real work and its partial state is the evidence for `S76` / `F70`.

**Do not score this directory.** It has 789 variant rows over 263 rsIDs, no compiled artifact, and a
`verification.json` attesting bytes that were never finished. Its `resolution.csv` has been removed —
captured first at `../evidence-S76-partial-resolution/`, hash-verified before deletion.

The clean re-run of the same paper is `run-ards-a/`.
