---
description: Check what a module claims against what the sources say
argument-hint: [spec directory]
---

Load the `module-check` skill and follow it for the module at $ARGUMENTS (ask if not given). Report warnings on a green run — they are the interesting output — and never present a check that could not run as a check that passed.

The skill is the procedure; this command only routes to it. Do not restate `module-check`'s content here or work from memory of it — load it.
