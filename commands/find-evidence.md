---
description: Search the literature, verify a PMID, and get an article's full text
argument-hint: [a question, variant, gene, trait, PMID, or DOI]
---

Load the `find-evidence` skill and follow it for: $ARGUMENTS

Take every PMID from a search result, never from memory — a recalled PMID is usually a real record for a different paper, and only a title settles identity.

The skill is the procedure; this command only routes to it. Do not restate `find-evidence`'s content here or work from memory of it — load it.
