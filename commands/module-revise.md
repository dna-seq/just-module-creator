---
description: Open a module again — second, third or twenty-fifth pass
argument-hint: [spec directory, and what changed]
---

Load the `module-revise` skill and follow it for: $ARGUMENTS

The output is a **decision list**, not a diff and not a findings dump. An old module is out of date, not broken — apply the evident and mechanical silently, and surface only what a human must choose.

The skill is the procedure; this command only routes to it. Do not restate `module-revise`'s content here or work from memory of it — load it.
