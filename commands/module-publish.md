---
description: Rehearse on the polygon, then publish to the catalog
argument-hint: [spec directory]
---

Load the `module-publish` skill and follow it for the module at $ARGUMENTS (ask if not given).

Publish to the **polygon** unless the user has explicitly asked for the official catalog. A first-timer saying 'put it somewhere my friends can see it' means the polygon; production is immutable and a half-baked module cannot be taken back.

The skill is the procedure; this command only routes to it. Do not restate `module-publish`'s content here or work from memory of it — load it.
