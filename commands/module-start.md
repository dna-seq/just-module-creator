---
description: Triage sources and scaffold a new module spec directory
argument-hint: [theme, gene, trait, or paths to sources]
---

Load the `module-start` skill and follow it. $ARGUMENTS is whatever the user brought — a theme, a gene, a trait, some PDFs or links, or nothing yet. Their sources and their theme are the input; the triage, the licence question and the spec are yours. Ask the questions that skill says to ask once, rather than one at a time.

The skill is the procedure; this command only routes to it. Do not restate `module-start`'s content here or work from memory of it — load it.
