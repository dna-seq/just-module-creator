---
description: What a just-dna module is and what this plugin can do — start here
---

Load the `module-101` skill and orient the user with it: what a just-dna annotation module is, what this plugin can and cannot do, and which stage skill owns the step they are on. If they have already described a task, route them to that stage rather than reciting the map at them.

The skill is the procedure; this command only routes to it. Do not restate `module-101`'s content here or work from memory of it — load it.
