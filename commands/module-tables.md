---
description: Which table kind does this finding belong in?
argument-hint: [describe the finding]
---

Load the `module-tables` skill and route: $ARGUMENTS

The question is what the row's *subject* is, not what data happens to be in hand. Ask the tool for columns rather than reciting them.

The skill is the procedure; this command only routes to it. Do not restate `module-tables`'s content here or work from memory of it — load it.
